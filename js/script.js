/* ====================================
   Onload functions
   ==================================== */

$(document).ready(function($) {

  BP.Foo.init({
    trigger: $('#foo'),
    element: $('#bar')
  });

});